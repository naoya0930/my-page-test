# 画面仕様書 - The Artist's Way Support (v2.0)

> **更新日**: 2026年6月25日  
> **ベース**: `docs/202606_spec2/screen_spec.md`

---

## 目次

- [1. 画面一覧・ルーティング](#1-画面一覧ルーティング)
- [2. 画面遷移図（フロー）](#2-画面遷移図フロー)
- [3. 各画面詳細仕様](#3-各画面詳細仕様)
  - [3.1 LP (ランディングページ)](#31-lp-ランディングページ)
  - [3.2 ログイン画面](#32-ログイン画面)
  - [3.3 ユーザーホーム画面](#33-ユーザーホーム画面)
  - [3.4 モーニングページ画面](#34-モーニングページ画面)
  - [3.5 アーティストデート画面](#35-アーティストデート画面)
  - [3.6 統計画面（新規）](#36-統計画面新規)
- [4. バックエンドAPIおよびデータモデル](#4-バックエンドapiおよびデータモデル)
- [5. 共通デザイントークン](#5-共通デザイントークン)

---

## 1. 画面一覧・ルーティング

| パス | 画面名 | 概要 | 認証制限 |
| :--- | :--- | :--- | :--- |
| `/` | LP (ランディングページ) | アプリの名称・概要説明、ログインへの誘導 | 不要 |
| `/login` | ログイン画面 | Supabase Authを利用したサインイン画面 | 不要 |
| `/home` | ユーザーホーム画面 | 現在・過去・未来の週次進捗確認、各タスクへのリンク | **必須** |
| `/morning-page` | モーニングページ画面 | 毎日の朝のノート（テキスト）入力・文字数カウント・保存 | **必須** |
| `/artist-date` | アーティストデート画面 | 週次のアーティストデート実施チェック・保存 | **必須** |
| `/statistics` | 統計画面（新規） | 12週間プログラム全体の入力傾向・アクティビティ可視化 | **必須** |

---

## 2. 画面遷移図（フロー）

### 未認証ユーザーのアクセス
- どのアドレスにアクセスしても、`/` または `/login` 以外はフロントエンドで捕捉し、`/login` へリダイレクト。

### ログインフロー
- `/login` で認証成功 ➔ `/home` へ遷移。

### ホームからの遷移
- `/home` ➔ [モーニングページ入力] ➔ `/morning-page`
- `/home` ➔ [アーティストデート入力] ➔ `/artist-date`
- `/home` ➔ [統計を見る] ➔ `/statistics`

### タスク完了後のフロー
- `/morning-page` ➔ [保存] ➔ 成功時：状態を即時更新して `/home` へ戻る（または同画面で完了通知後、同期を維持）
- `/artist-date` ➔ [保存] ➔ 成功時（安全に保管された場合）：**自動的に `/home` へ遷移する**（追加要件）

### 共通ナビゲーション
- 認証後の全画面において、ヘッダー等の共通コンポーネントから `/home` および `/statistics` への相互遷移が可能。

---

## 3. 各画面詳細仕様

### 3.1 LP (ランディングページ)

**パス**: `/`  
**認証**: 不要（公開）  
**ファイル**: `src/pages/LandingPage.tsx`

#### 表示内容
- アプリの名称と12週間プログラムをサポートする旨の簡潔な説明。
- 書籍本文の具体的な引用は行わず、抽象的な機能紹介にとどめる。
- ブラウザ通知 / リマインダー機能の紹介文。

#### UI/アクション
- 中央に目立つ「ログインへ」のCTAボタンを配置。クリックで `/login` へ遷移。

#### レイアウト（参考）
```
┌─────────────────────────────────────┐
│ The Artist's Way Support            │
│                                     │
│ 12週間のモーニングページとアーティスト │
│ デートを習慣化するサポートアプリです。  │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ 主な機能                         │ │
│ │ • 毎日のモーニングページ保存       │ │
│ │ • 今週のアーティストデート記録     │ │
│ │ • ホームで週次進捗を確認           │ │
│ │ • 統計画面で12週間の軌跡を可視化   │ │
│ └─────────────────────────────────┘ │
│                                     │
│ [ログインへ →]                       │
└─────────────────────────────────────┘
```

---

### 3.2 ログイン画面

**パス**: `/login`  
**認証**: 不要（認証済みなら `/home` へリダイレクト）  
**ファイル**: `src/pages/LoginPage.tsx`

#### 表示内容
- Supabase Authと連携したログインフォーム（メール/パスワード、またはソーシャルログイン）。

#### 挙動・セキュリティ
- 認証成功時、アクセストークンをフロントエンド（状態/ストレージ）に保持。
- ログインエラー時は、フォーム下部にエラーメッセージ（「メールアドレスまたはパスワードが違います」等）を即時表示。

---

### 3.3 ユーザーホーム画面

**パス**: `/home`  
**認証**: 必要（未認証なら `/login` へリダイレクト）  
**ファイル**: `src/pages/HomePage.tsx`  
**API**: `GET /api/progress?week_number=N`

ユーザーがプログラムの進捗を確認し、その日のアクションを起こすための中心的な画面です。

#### ① 週次表示とタイムトラベル（左右スワイプ）機能（追加要件）

##### 初期表示
- アクセス時、ユーザーの開始日と現在のシステム日付から算出された**「現在の自身の経過週（例：第3週目）」**をデフォルトで中央に表示する。

##### 左右スワイプ / ナビゲーション
- 画面を左右にスワイプ（または左右の矢印ボタン「＜」「＞」をクリック）することで、過去の週（第1週〜現在未満）や未来の週（現在より上〜第12週まで）の状態に表示を切り替えられる。

##### 「いまではないもの」を見ている際の視覚的演出（追加要件）
- 現在の表示週が「現在の本人の経過週」と異なる場合、UIの色調を明確に変更し、過去や未来のデータを見ている（タイムトラベル中である）ことをユーザーに認識させる。

**演出案:**
- **過去の週を表示中:** 画面全体の色彩を「セピア」または「モノトーン（グレースケール）」に変更。各入力ボタンは非活性（閲覧のみ）。
- **未来の週を表示中:** 画面全体に「半透明のオーバーレイ」または「ソフトブルーのグラデーション」を適用し、未来のプレースホルダーであることを明示。タスクはロック状態とする。
- **現在週を表示中:** アプリケーションの通常ブランドカラー（高コントラスト、アクティブ表示）で表示。

#### ② モーニングページ・日次実行ステータス（追加要件）

##### 表示デザイン
- 今週（表示中の週）の7日間において、何日目に実行し、何日目に実行していないのかがひと目で直感的にわかるデザインを採用する。

**デザイン案（ドットグリッド / カレンダーマトリクス）:**
- 横一列に7つの円（ドット）またはカードを配置（Day 1 〜 Day 7）。
- **実行済みの日の表示:** 鮮やかな成功カラー（例: ターコイズグリーン、チェックマーク付きの塗りつぶし）で表示。
- **未実行（かつ過去・当日）の日の表示:** グレーアウト、または破線の枠線のみで表現し、スキップされたか未着手であることを明示。
- 各ドットの下部には、その日に執筆した「文字数」を小さく表示する。

##### リンク
- 当日のドット、または「今日のモーニングページを書く」ボタンから `/morning-page` へ遷移。

#### ③ アーティストデート進捗表示

##### 表示内容
- 今週分の実施状況をチェックリスト形式で表示。
  - 「今週、アーティストデートに出かけた」
  - 「今週、わくわくした出来事があった」

##### リンク
- クリックで `/artist-date` へ遷移。

#### ④ バグ修正：編集後のステータス同期（追加要件）

##### 修正要件
- モーニングページを新規作成、または過去の記述を編集・保存した後にホーム画面に戻った際、進捗状態（`Progress` テーブルおよびフロントエンド状態）が正しく反映されず、タスクが「未完了」のままになってしまうバグを完全に修正する。

##### 実装上の挙動
- 保存処理が成功した段階で、フロントエンドの状態管理（React Context / Redux / SWR / TanStack Query等）のキャッシュを強制的に無効化（Invalidate）するか、最新の Progress API を再フェッチ（Refetch）する。
- ホーム画面のコンポーネントがマウントされた際、常に最新のDB状態（当日分の `MorningPages` レコードの有無）を参照し、「完了」ステータスへの切り替えを遅延なく行う。

#### レイアウト（参考）
```
┌────────────────────────────────────────┐
│ ホーム        [統計] [ログアウト]       │
│ user@example.com                       │
└────────────────────────────────────────┘

   ＜  [第N週目]  ＞
   (スワイプ可能、色調は現在/過去/未来で変化)

┌────────────────────────────────────────┐
│ The Artist's Way プログラム            │
│ 第N週目 / 全12週間                    │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ モーニングページ (今週の実行状況)       │
│ ●●●○○○○  (7日間のドット)            │
│ Day1 Day2 Day3 Day4 Day5 Day6 Day7    │
│ 750  820  0    0    0    0    0      │
│ (文字数表示)                           │
└────────────────────────────────────────┘

┌──────────────────┐ ┌──────────────────┐
│ モーニングページ→ │ │ アーティストデート→│
│ 今日の思考を記録  │ │ 今週のクリエイティ │
│                  │ │ ブな時間を記録    │
└──────────────────┘ └──────────────────┘
```

---

### 3.4 モーニングページ画面

**パス**: `/morning-page`  
**認証**: 必要  
**ファイル**: `src/pages/MorningPage.tsx`  
**API**: `GET /api/morning-pages?date=YYYY-MM-DD`, `POST /api/morning-pages`

#### 表示内容
- 広範なテキスト入力欄（マルチラインのTextArea、フォーカス時に枠線強調）。
- **リアルタイム文字数カウント:** 改行、空白スペースを含むすべての入力文字数をミリ秒単位でクライアントサイドでカウントし、「〇〇文字」とリアルタイム表示。
- 「保存」ボタンおよび「ホームへ戻る」リンク。

#### バリデーション & 挙動
- テキストが空（0文字）の場合は、保存ボタンを非活性化するか、押下時に「内容を入力してください」と警告を表示してAPI送信をブロックする。
- 保存ボタン押下時、バックエンド（Cloudflare Workers）の `POST /api/morning-pages` へ `entry_date`, `content`, `character_count` を送信。成功時はトースト通知を表示し、ステータスを即時同期（3.3-④を参照）。

#### レイアウト（参考）
```
モーニングページ          [← ホームに戻る]
今日の思いを自由に書いてください

┌──────────────────────────────────────┐
│ [成功/エラーメッセージ] (条件表示)     │
│                                      │
│ ┌────────────────────────────────┐   │
│ │                                │   │
│ │  (テキストエリア h-96)          │   │
│ │                                │   │
│ └────────────────────────────────┘   │
│                                      │
│ N 文字（目安: 750文字以上）   [保存]   │
│                                      │
│ 💡 モーニングページのコツ              │
│ • 毎朝、起きてすぐに書くのが効果的      │
│ • 思いつくままに、何でも書いてOK       │
└──────────────────────────────────────┘
```

---

### 3.5 アーティストデート画面

**パス**: `/artist-date`  
**認証**: 必要  
**ファイル**: `src/pages/ArtistDatePage.tsx`  
**API**: `GET /api/progress`, `GET /api/artist-dates?week_number=N`, `POST /api/artist-dates`

#### 表示内容
- 2つの独立したトグルスイッチまたはチェックボックス。
  1. 「今週、アーティストデートに出かけた」 (`went_out`: boolean)
  2. 「今週、わくわくした出来事があった」 (`excited`: boolean)
- 「保存」ボタン。
- 画面下部に、直近に保存されたステータス（同期時刻など）を表示。

#### 保存時の遷移挙動の変更（追加要件）
- ユーザーが「保存」ボタンを押下する。
- バックエンドAPI（`POST /api/artist-dates`）への送信が走り、データベース（D1）への書き込みが正常に完了して `200 OK` が返却される。
- **安全に保管されたことが確認できた場合、画面内に留まらず、自動的にユーザーホーム画面（`/home`）にリダイレクト（遷移）する。**
- 保存失敗時は、エラーメッセージ「保存に失敗しました。再度お試しください。」を表示し、画面を遷移させずにユーザーの再試行を促す。

#### レイアウト（参考）
```
アーティストデート        [← ホームに戻る]
第N週目のアーティストデート記録

┌────────────────────────────────────────┐
│ [成功/エラーメッセージ] (条件表示)      │
│                                        │
│ ☐ 今週、アーティストデートに出かけた    │
│   一人で美術館、カフェ、散歩など...     │
│                                        │
│ ☐ 今週、わくわくした出来事があった      │
│   新しい発見や、心が躍るような...       │
│                                        │
│                              [保存]    │
│ (保存成功後は自動的に /home へ遷移)    │
│                                        │
│ 💡 どちらも未選択でも保存できます。     │
└────────────────────────────────────────┘
```

---

### 3.6 統計画面（新規）

**パス**: `/statistics`  
**認証**: 必要  
**ファイル**: `src/pages/StatisticsPage.tsx`（新規作成）  
**API**: `GET /api/statistics`

ユーザーが12週間のプログラムにおいて、どのような軌跡をたどってきたか（継続の証、自己の思考パターンの変化）を定量的に振り返るための画面です。

#### 表示内容

##### 1. 12週間総合サマリーカード
- モーニングページの総執筆日数（例：52日 / 84日中）
- 累計総文字数（例：68,500 文字）
- アーティストデート累計達成率（例：8週 / 12週中）

##### 2. 12週間の執筆文字数推移（グラフエリア）
- 横軸を第1週〜第12週（Week 1 〜 Week 12）とし、縦軸を各週の合計文字数、または平均文字数とした棒グラフ（またはラインチャート）を表示。
- まだ到達していない未来の週は、グラフ上で「未到達（Pending）」または非表示として扱う。

##### 3. 日次アクティビティヒートマップ
- GitHubのコントリビューショングラフ（草）のように、12週間×7日間（＝84マス）のマトリクスを表示。
- モーニングページを実行した日は文字数に応じて濃い色で塗りつぶされ、実行していない日は空白/薄いグレーで表示されることで、自身の「12週間の記載のムラやリズム」を視覚的に捉えられるようにする。

##### 4. アーティストデートの軌跡
- 第1週〜第12週それぞれの「出かけた (`went_out`)」「わくわくした (`excited`)」の達成状況をアイコンのオン/オフで一覧表示。

#### レイアウト（参考）
```
統計                    [← ホームに戻る]
あなたの12週間の軌跡

┌────────────────────────────────────────┐
│ 12週間総合サマリー                     │
│ • 総執筆日数: 52日 / 84日中            │
│ • 累計総文字数: 68,500 文字            │
│ • アーティストデート達成: 8週 / 12週中 │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ 週次文字数推移グラフ                    │
│ (Week 1〜Week 12の棒グラフ)            │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ 日次アクティビティヒートマップ           │
│ (84マス: 12週×7日のGitHub草風)         │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│ アーティストデート軌跡                  │
│ Week1: ✓出 ✓わく                      │
│ Week2: ✓出 ○わく                      │
│ ...                                    │
└────────────────────────────────────────┘
```

---

## 4. バックエンドAPIおよびデータモデル

仕様変更に伴い、以下のデータ・ロジックの保証が必要です。

### 4.1 `GET /api/progress` の拡張
- 特定の `week_number` をパラメータとして受け取り、その週に紐づく `Progress` 状況および `MorningPages`（7日間分の日次有無）、`ArtistDates` のフラグを一括で返却する設計にする（スワイプ時の高速描画のため）。

**エンドポイント:**
```
GET /api/progress?week_number=3
```

**レスポンス例:**
```json
{
  "week_number": 3,
  "current_week": 3,
  "morning_pages_this_week": 5,
  "artist_date_done": true,
  "daily_status": [
    { "day": 1, "date": "2024-01-15", "done": true, "character_count": 750 },
    { "day": 2, "date": "2024-01-16", "done": true, "character_count": 820 },
    { "day": 3, "date": "2024-01-17", "done": true, "character_count": 680 },
    { "day": 4, "date": "2024-01-18", "done": true, "character_count": 900 },
    { "day": 5, "date": "2024-01-19", "done": true, "character_count": 740 },
    { "day": 6, "date": "2024-01-20", "done": false, "character_count": 0 },
    { "day": 7, "date": "2024-01-21", "done": false, "character_count": 0 }
  ],
  "artist_date": {
    "went_out": true,
    "excited": true
  }
}
```

### 4.2 `GET /api/statistics` の新設
- ユーザーIDに紐づくすべての過去ログを走査し、週ごとの文字数集計、総日数、各フラグの成立数を集計してフロントエンドに返す軽量な集計クエリ（SQLの `GROUP BY week_number` 等を活用）を Cloudflare Workers 上に実装する。

**エンドポイント:**
```
GET /api/statistics
```

**レスポンス例:**
```json
{
  "summary": {
    "total_days": 52,
    "total_days_possible": 84,
    "total_characters": 68500,
    "artist_date_weeks": 8,
    "artist_date_weeks_possible": 12
  },
  "weekly_stats": [
    { "week_number": 1, "total_characters": 5600, "days_written": 6 },
    { "week_number": 2, "total_characters": 4200, "days_written": 5 },
    ...
  ],
  "daily_activity": [
    { "date": "2024-01-01", "character_count": 750, "week_number": 1, "day_of_week": 1 },
    { "date": "2024-01-02", "character_count": 820, "week_number": 1, "day_of_week": 2 },
    ...
  ],
  "artist_date_history": [
    { "week_number": 1, "went_out": true, "excited": true },
    { "week_number": 2, "went_out": true, "excited": false },
    ...
  ]
}
```

### 4.3 トランザクション/更新トリガーの適正化
- `MorningPages` の保存・更新時に、該当する週の `Progress.morning_page_done` を即座に評価・更新するロジックをバックエンド側でも担保し、フロントエンドへの一貫したデータ返却を行う。
- `ArtistDates` の保存時も同様に `Progress.artist_date_done` を更新する。

---

## 5. 共通デザイントークン

| トークン | 値 | 備考 |
|---------|-----|------|
| プライマリカラー | `indigo-600` (#4F46E5) | 現在週の通常表示 |
| ホバー | `indigo-700` | |
| 背景 | `slate-50` | |
| カード背景 | `white` | |
| カード角丸 | `rounded-3xl` | |
| カードボーダー | `border border-slate-200` | |
| カードシャドウ | `shadow-sm` | |
| テキスト（メイン） | `slate-900` | |
| テキスト（サブ） | `slate-600` | |
| テキスト（補足） | `slate-500` | |
| エラー背景 | `red-50` / テキスト `red-700` | |
| 成功背景 | `emerald-50` / テキスト `emerald-700` | |
| ヒント背景 | `indigo-50` / テキスト `indigo-800` | |
| 注意背景 | `amber-50` / テキスト `amber-900` | |
| ボタン角丸 | `rounded-full` | |
| **過去週演出** | `grayscale` / `sepia` | グレースケールまたはセピア |
| **未来週演出** | `opacity-50` / `blur-sm` | 半透明＋ブラー、ソフトブルー |
| **成功ドット** | `bg-teal-500` / `bg-emerald-500` | ターコイズグリーン系 |
| **未実行ドット** | `bg-slate-200` / `border-dashed` | グレーアウト |

---

## 変更履歴

| 日付 | バージョン | 変更内容 |
|------|----------|---------|
| 2026-06-25 | v2.0 | 新仕様対応（202606_spec2/screen_spec.md）<br>- 統計画面追加<br>- ホーム画面のタイムトラベル機能<br>- 日次ステータス表示<br>- 視覚的演出追加<br>- アーティストデート自動リダイレクト<br>- バックエンドAPI拡張 |
| 2026-06-23 | v1.0 | 初版作成（MVP完成時） |

---

**注記**: この仕様書は実装の指針となるドキュメントです。実装時には技術的制約やUX改善の観点から、細部が調整される可能性があります。
