# TODOリスト

## 0. notion
ALL tasks depend on system_design.md, before you do tasks, plz read this file.
Your task process log is written in gen_logs/.
## 1. 環境構築

- [x] `package.json` と `vite.config.ts` を確認し、React + TypeScript の開発環境が整っていることを確認する
- [x] Docker 環境で依存関係がセットアップされていることを確認する（`docker-compose.yml` / `dockerfile` の内容確認）
- [x] `docker-compose up --build` で開発環境が立ち上がることを確認する
- [x] Cloudflare Workers と D1 を Docker から起動・デバッグできる設定があるか確認する（wrangler.toml など）
- [x] `src/` に `api/`、`components/`、`pages/` フォルダを作成する
- [x] `docs/system_design.md` をベースに、実装対象画面と API を一覧化する

## 2. DB設計・バックエンド準備

- [x] D1 で作成するスキーマを `migrations/` または `schema.sql` に定義する
- [x] `Users` テーブルのカラムを定義する: `id`, `supabase_user_id`, `email`, `created_at`, `last_active_at`, `auth_provider`
- [x] `MorningPages` テーブルのカラムを定義する: `id`, `user_id`, `entry_date`, `content`, `character_count`, `created_at`, `updated_at`
- [x] `ArtistDates` テーブルのカラムを定義する: `id`, `user_id`, `week_number`, `went_out`, `excited`, `created_at`, `updated_at`
- [x] `Progress` テーブルを補助集計用に定義する: `id`, `user_id`, `week_number`, `morning_page_done`, `artist_date_done`, `updated_at`
- [x] Cloudflare Workers で D1 接続を行う API 公開の雛形ファイルを作成する
- [x] Supabase Auth トークン検証の雛形を Workers のエンドポイントに追加する
- [ ] Supabase Auth は email ログインと Google OIDC ログインを許可する前提で設計する

## 3. バックエンド API 実装

- [x] `POST /api/morning-pages` エンドポイントを実装し、モーニングページを保存できるようにする
- [x] `GET /api/morning-pages?date=YYYY-MM-DD` エンドポイントを実装し、指定日付のモーニングページを取得できるようにする（✅ content フィールド正常動作確認済み）
- [x] `POST /api/artist-dates` エンドポイントを実装し、今週のアーティストデート記録を保存できるようにする
- [x] `GET /api/artist-dates?week_number=N` エンドポイントを実装し、週別のアーティストデートを取得できるようにする
- [x] `GET /api/progress` エンドポイントを実装し、ユーザーホームの現在週次進捗を返すようにする
- [x] 各 API で `supabase_user_id` を認証済みユーザーに紐づけて処理し、`user_id` は Workers 側で解決するロジックを実装する
- [x] エラー時に適切な JSON 形式のメッセージを返すようにする
- [x] バックエンドのテスト用データ挿入スクリプトを用意する (`migrations/test_data.sql`)
- [ ] APIにトークンを送付し、サンプルユーザでログインできることを確認する（ユーザ情報は、`.env`に存在）

## 4. フロントエンド共通基盤

- [x] `src/main.tsx` にルーティングを追加し、`/`, `/login`, `/home`, `/morning-page`, `/artist-date` のパスを定義する
- [x] `src/App.tsx` にルート切り替えと認証状態管理の仕組みを実装する
- [x] 認証状態を保持するコンテキストまたは状態管理を `src/auth/` に実装する
- [x] API 呼び出し用の `src/api/` ユーティリティを作成し、共通の fetch ロジックと Supabase Auth トークン送信を定義する (`src/api/client.ts`, `src/auth/supabaseClient.ts`, `src/vite-env.d.ts` 作成完了、`@supabase/supabase-js` インストール済み)
- [x] `src/components/Layout.tsx` に共通ヘッダーとフッターのレイアウトコンポーネントを作成する

## 5. フロントエンド画面実装

### 5.1 LP

- [x] `src/pages/LandingPage.tsx` を作成する
- [x] アプリ名、説明文、通知機能の紹介、ログインへの CTA を表示する
- [x] CTA クリックで `/login` に遷移するようにする

### 5.2 ログイン画面

- [x] `src/pages/LoginPage.tsx` を作成する
- [x] Supabase Auth クライアントを利用した認証ボタンを配置する（メールログイン＋Googleログイン実装完了）
- [x] 認証成功時に `/home` に遷移するロジックを実装する
- [x] 認証失敗時にエラーメッセージを表示する
- [x] 未認証ユーザーから `/home`, `/morning-page`, `/artist-date` へのアクセスをガードする（AuthProvider.tsxでセッション管理実装済み）

### 5.3 ユーザーホーム画面

- [x] `src/pages/HomePage.tsx` を作成する
- [x] `GET /api/progress` を呼び出して現在週次とチェックリスト状態を取得する（progressApi.get() 実装完了）
- [x] 「現在第◯週目」を表示する UI を作成する（グラデーション付き週次カード実装）
- [x] 「モーニングページ」「アーティストデート」リンクを配置する（アクションカード実装）
- [x] モーニングページ入力済み／アーティストデート完了のチェックリストを表示する（進捗サマリーカード実装）
- [x] データ取得中はローディング表示を出す（スピナー付きローディング実装）
- [x] API 取得失敗時のエラー表示を用意する（エラー表示＋再読み込みボタン実装）
- [x] ユーザー情報とログアウトボタンを追加（ヘッダーカード実装）
- [x] 週次進捗バーを実装（モーニングページ 7/7 進捗バー）

### 5.4 モーニングページ画面

- [x] `src/pages/MorningPage.tsx` を作成する
- [x] マルチラインのテキストエリアを用意する（高さ96のtextarea実装）
- [x] 入力内容を state で管理し、リアルタイムで文字数を計算する（charCount実装）
- [x] 文字数表示を「0文字」から更新するロジックを実装する（toLocaleString()でカンマ区切り表示）
- [x] `保存` ボタンを配置し、クリック時に `POST /api/morning-pages` を実行する（morningPagesApi.create()実装）
- [x] 保存成功時に「保存しました」のメッセージを表示する（3秒後自動非表示）
- [x] 保存失敗時に「保存に失敗しました。再度お試しください。」を表示する（エラーハンドリング実装）
- [x] 保存時に入力が空の場合はバリデーションエラーとして保存を防ぐ（content.trim()チェック実装）
- [x] 画面表示時に当日の既存保存データがあれば読み込み、テキストエリアに反映する（useEffect + morningPagesApi.get()実装）
- [x] ローディング状態とスピナー表示を実装
- [x] 保存中状態の管理とボタン無効化を実装
- [x] 「ホームに戻る」リンクを追加
- [x] モーニングページのコツ（Tips）を表示

### 5.5 アーティストデート画面

- [x] `src/pages/ArtistDatePage.tsx` を作成する
- [x] 「今週出かけたか」「わくわくしたか」チェックボックスを作成する（説明文付き）
- [x] チェック状態を state で管理する（wentOut, excited）
- [x] `保存` ボタンを配置し、クリック時に `POST /api/artist-dates` を実行する（artistDatesApi.create()実装）
- [x] 保存成功時に「保存しました」のメッセージを表示する（3秒後自動非表示）
- [x] 保存失敗時に「保存に失敗しました。再度お試しください。」を表示する（エラーハンドリング実装）
- [x] 画面表示時に今週の既存保存データがあれば読み込み、チェック状態を復元する（useEffect + progressApi + artistDatesApi実装）
- [x] どちらも未選択でも保存できるようにする（バリデーションなし、注意書き表示）
- [x] ローディング状態とスピナー表示を実装
- [x] 保存中状態の管理とボタン無効化を実装
- [x] 現在週次を表示
- [x] 「ホームに戻る」リンクを追加
- [x] アーティストデートの説明（Tips）を表示

## 6. 結合テストと確認

- [x] ローカル開発環境で `docker compose ~` を起動し、アプリ全体がビルドできることを確認する（Web: 5173, Workers: 8787 正常起動）
- [x] LP からログイン、ホーム、モーニングページ、アーティストデートと遷移できることを確認する（統合テストで検証済み）
- [x] モーニングページの文字数カウントが動作することを確認する（✅ 92文字カウント正確）
- [x] モーニングページ保存後にバックエンドにデータが保存されることを確認する（✅ POST成功、GETで取得確認）
- [x] アーティストデート保存後にバックエンドにデータが保存されることを確認する（✅ POST成功、GETで取得確認）
- [x] `GET /api/progress` でホームのチェックリストが正しく表示されることを確認する（✅ 週次・進捗データ取得成功）
- [x] 未認証状態で保護ページにアクセスすると `/login` にリダイレクトされることを確認する（✅ 401エラー正常動作）
- [x] 統合テストスクリプト（integration-test.js）作成・実行（全8テスト成功 🎉）
- [x] APIクライアントを環境変数対応に修正（本番デプロイ対応: VITE_API_BASE_URL）

## 7. ドキュメント・改善

- [x] `docs/system_design.md` で定義した要件と実装が整合しているか確認する（✅ ギャップ分析完了: MVP範囲97%実装済み）
- [x] 必要に応じて `docs/todo.md` を更新し、追加フェーズや未確定要素を追記する（✅ 将来タスク文書化完了）
- [x] 認証方式や通知機能など未確定要素を別 Issue またはタスクとして切り出す（✅ 10個のIssue整理完了）
- [x] ギャップ分析ドキュメント作成（`gen_logs/7/gap_analysis.md`）
- [x] 将来タスク・Issue文書作成（`gen_logs/7/future_tasks.md`）

## 8. 新仕様対応（202606_spec2/screen_spec.md）

### 8.1 ルーティング拡張

- [x] `src/main.tsx` に `/statistics` のルートを追加する（App.tsxに実装済み）
- [x] 統計画面への認証ガードを設定する（ProtectedRouteで実装済み）

### 8.2 バックエンドAPI拡張

- [x] `GET /api/progress` を拡張し、`week_number` パラメータを受け取れるようにする
- [x] `GET /api/progress?week_number=N` で指定週の進捗・モーニングページ7日間分・アーティストデート状態を返すようにする
- [x] `GET /api/statistics` エンドポイントを新設する
  - 12週間総合サマリー（総執筆日数、累計総文字数、アーティストデート達成率）
  - 週ごとの文字数集計（GROUP BY week_number）
  - 日次アクティビティデータ（84日分）
  - アーティストデート週次達成状況
- [x] `POST /api/morning-pages` 保存時に `Progress.morning_page_done` を自動更新するロジックを実装する
- [x] `POST /api/artist-dates` 保存時に `Progress.artist_date_done` を自動更新するロジックを実装する

### 8.3 ユーザーホーム画面の大幅改修

#### 8.3.1 週次タイムトラベル機能

- [x] 週次表示に左右スワイプ（またはボタン「＜」「＞」）機能を実装する
- [x] 現在の経過週を初期表示として算出する
- [x] 第1週〜第12週までの切り替えを可能にする
- [x] 表示中の週番号を状態管理（useState）する

#### 8.3.2 視覚的演出（タイムトラベル中の表現）

- [x] **過去週表示時**: セピア/モノトーン（グレースケール）のスタイル適用
- [x] **過去週表示時**: 入力ボタンを非活性化（閲覧のみモード）
- [x] **未来週表示時**: 半透明オーバーレイ/ソフトブルーグラデーション適用
- [x] **未来週表示時**: タスクをロック状態で表示
- [x] **現在週表示時**: 通常ブランドカラー（高コントラスト）で表示

#### 8.3.3 モーニングページ日次ステータス表示

- [x] 7日間のドットグリッド（Day 1〜Day 7）をデザイン・実装する
- [x] 実行済みの日: 鮮やかな成功カラー（ターコイズグリーン、チェックマーク）で表示
- [x] 未実行の日: グレーアウト/破線枠で表示
- [x] 各ドットの下部に文字数を小さく表示する
- [x] 当日のドットまたはボタンから `/morning-page` への遷移を実装（Linkカード経由）

#### 8.3.4 バグ修正: 編集後のステータス同期

- [x] モーニングページ保存後、フロントエンドのキャッシュを無効化（Invalidate）する
- [x] ホーム画面マウント時に最新のDB状態を取得するよう修正する（useEffect実装）
- [x] Progress APIの再フェッチ（Refetch）ロジックを実装する（fetchProgress関数）
- [x] 保存成功時の状態同期を遅延なく行うよう改善する（ArtistDatePageで自動リダイレクト実装済み）

#### 8.3.5 共通ナビゲーション

- [x] ヘッダーに `/home` と `/statistics` への相互遷移リンクを追加する

### 8.4 アーティストデート画面の改修

- [x] 保存成功時に自動的に `/home` へリダイレクトする機能を実装する
- [x] 保存失敗時は画面遷移せず、エラーメッセージを表示して再試行を促す

### 8.5 統計画面の新規実装

- [x] `src/pages/StatisticsPage.tsx` を新規作成する

#### 8.5.1 12週間総合サマリーカード

- [x] モーニングページの総執筆日数を表示（例: 52日 / 84日中）
- [x] 累計総文字数を表示（例: 68,500 文字）
- [x] アーティストデート累計達成率を表示（例: 8週 / 12週中）

#### 8.5.2 12週間の執筆文字数推移グラフ

- [x] グラフライブラリ（Recharts / Chart.js）を選定・インストールする（CSSベースの棒グラフで実装）
- [x] 横軸: Week 1〜Week 12、縦軸: 各週の合計/平均文字数の棒グラフを実装する
- [x] 未到達の未来週は「Pending」または非表示として扱う

#### 8.5.3 日次アクティビティヒートマップ

- [x] 12週間×7日間（84マス）のマトリクスUIを実装する
- [x] GitHub草グラフ風のデザインを適用する
- [x] モーニングページ実行日は文字数に応じて濃い色で表示
- [x] 未実行日は空白/薄いグレーで表示
- [x] マウスホバーで詳細（日付・文字数）を表示する

#### 8.5.4 アーティストデート軌跡表示

- [x] 第1週〜第12週の `went_out` と `excited` の達成状況を一覧表示する
- [x] アイコンのオン/オフで視覚化する

### 8.6 共通コンポーネント改修

- [x] `src/components/Layout.tsx` に統計画面へのリンクを追加する
- [x] ヘッダーのナビゲーションメニューを拡張する

### 8.7 テスト・検証（手動テスト項目）

**すべての実装タスクが完了しました。以下は手動で動作確認するためのチェックリストです。**

#### 手動テスト項目
- [x] **週次タイムトラベル機能**: ホーム画面で ◀ ▶ ボタンをクリックし、第1週〜第12週を切り替えられることを確認 ✅
- [x] **視覚的演出（色調変化）**: 過去週（グレースケール）、現在週（カラフル）、未来週（ブルートーン）の表示確認 ✅
- [x] **モーニングページ日次ステータス表示**: 7日間のドットグリッドに完了日（✓）と文字数が表示されることを確認 ✅
- [x] **統計画面のデータ表示**: `/statistics` でサマリー、週次グラフ、84日ヒートマップ、アーティストデート軌跡が表示されることを確認 ✅
- [x] **アーティストデート保存後の自動リダイレクト**: 保存ボタンクリック後に `/home` に自動遷移することを確認 ✅
- [x] **ステータス同期バグの修正**: モーニングページ保存後、ホーム画面に戻ると即座に進捗が反映されることを確認 ✅
- [x] **新規APIエンドポイント動作確認**: ✅
  - `GET /api/progress?week_number=N` で指定週のデータが取得できることを確認
  - `GET /api/statistics` で12週間のサマリーデータが取得できることを確認
- [x] **統合テストスクリプトの更新**: 新機能を含めた統合テストを作成・実行 ✅

#### テスト手順
1. ブラウザで `http://localhost:5173` にアクセス
2. ログイン（メールまたはGoogle）
3. 上記のチェックリストに従って各機能を確認
4. 問題があれば Issue として記録

## 9. 新仕様対応（202606_spec3/screen_spec.md / v2.1）

> ベース文書: `docs/202606_spec3/screen_spec.md`、反映先: `docs/screen_spec.md`（v2.2）

### 9.1 ログイン・アカウント作成画面の改修

- [x] `src/pages/LoginPage.tsx` に「ログイン」/「新規アカウント作成」のモード切り替えタブを実装する
- [x] アカウント作成フォーム（メール / パスワード / パスワード確認）を追加する
- [x] クライアントバリデーション（メール形式、パスワード一致）を実装する（メール形式は input type=email、パスワード一致＋6文字以上を検証）
- [x] Supabase Auth のサインアップAPIを呼び出す処理を実装する（AuthProvider に `signUpWithEmail` を追加）
- [x] サインアップ成功時に確認メール案内表示、または自動ログインして `/home` へ遷移する（session 有無で分岐）
- [x] サインアップ時のエラー（「既に登録されているメールアドレスです」等）を表示する（Supabase のエラーメッセージを表示）

### 9.2 ユーザーホーム画面の改修

- [x] 全体進捗インジケーター「Day XX / 84」を画面上部に表示する（`GET /api/progress` の `current_day` を追加して算出）
- [x] 7日間のドット（`dayX`）をクリック可能なボタンとして実装する
- [x] dayX クリックで `/morning-page?date=YYYY-MM-DD` へ遷移する
- [x] 過去日クリック時に確認ダイアログ（「過去のページです。編集を開始してよろしいですか？」）を表示し、OK時のみ遷移する
- [x] 未来日のドットはロック状態とし、クリック遷移を不可にする（disabled）
- [x] **日次ステータス同期**: モーニングページを保存（書き込み）した日は日次ステータス（色 / チェック）を「完了」に更新する（`daily_status[].done` が当日レコード有無を反映、ホーム再マウント時に再フェッチ）

### 9.3 モーニングページ画面の改修

- [x] `?date=YYYY-MM-DD` クエリを読み取り、対象日のデータを取得・表示する（未指定時は当日）
- [x] タイトルに対象日（当日は「今日のモーニングページ」、それ以外は「YYYY年M月D日の記録」）を表示する
- [x] 保存成功（`200 OK`）時に `/home` へ自動リダイレクトする
- [x] 保存失敗時は遷移せずエラーメッセージを表示して再試行を促す
- [x] バックエンド `POST /api/morning-pages` で任意の `entry_date`（YYYY-MM-DD、未来日不可）を受け付けるよう拡張



## 10. 新仕様対応（202606_spec4/direction.md / 画面仕様書 v2.3）

> ベース文書: `docs/202606_spec4/direction.md`、反映先: `docs/screen_spec.md`（v2.3）/ `docs/system_design.md`（v2.2）/ `docs/implementation_plan.md`
> **実装範囲メモ**: spec4 はすべて**フロントエンド完結**。新規APIやスキーマ変更は不要（完遂判定は既存 `GET /api/progress` の `current_day` を再利用）。

### 10.1 アカウント登録フローの改修（利用規約の同意必須化）

- [x] `src/pages/LoginPage.tsx` のサインアップモードに「利用規約およびプライバシーポリシーに同意する」チェックボックスを追加する
- [x] 同意が未チェックの間は登録ボタン（「アカウントを作成する」）を非活性（disabled）にする
- [x] 未同意のまま押下された場合のバリデーションエラー（「利用規約への同意が必要です」）を表示する
- [x] ログインモードではチェックボックスを表示しない（サインアップ時のみ）

### 10.2 プログラム完遂時の祝福ポップアップ

- [x] `GET /api/progress` の `current_day >= 84`（12週間完遂）を判定するロジックを実装する（`HomePage` 初回フェッチ時に判定）
- [x] 完遂時に祝福モーダル（「12週間のプログラム完遂、おめでとうございます！」）を表示する（`src/components/CompletionModal.tsx`）
- [x] 紙吹雪（Confetti）演出を実装する（CSSアニメーション、`prefers-reduced-motion` 配慮）
- [x] ログイン直後 / `/home` 遷移時に表示するトリガーを実装する（本バージョンは毎回表示・表示済みフラグ保存なし）
- [x] モーダルを「閉じる」でホーム画面に戻れるようにする

### 10.3 `/home`（ホーム画面）のUI改善

- [x] 全体進捗インジケーターの表記を「本日: Day X / 84」に調整する
- [x] モーニングページの今週の達成状況を「X/7」テキスト形式で併記する（例: 3/7）
- [x] 週移動ボタン（`<` / `>`）のクリック領域・サイズを広げ視認性を高める
- [x] 「現在の週に戻る」ボタンを過去/未来週の閲覧中のみ表示する（プライマリカラー、押下で現在週へ）
- [x] モーニングページ保存後の `/home` リダイレクト直後に「モーニングページを保存しました」トースト通知を表示する（数秒でフェードアウト）
- [x] リダイレクト時にナビゲーション state 等で保存成功を `/home` へ伝達し、リロードでトーストが再表示されないようにする

### 10.4 `/statistics`（統計画面）のUI改善

- [x] カレンダー/リストの各日をラジオボタンのように単一選択（アクティブ切り替え）できるUIにする（ヒートマップのマスを選択可能に）
- [x] 日付選択時に「詳細」ボタン（リンク）を表示する
- [x] 「詳細」押下で `/morning-page?date=YYYY-MM-DD` へ遷移し、該当日の内容を閲覧・編集できるようにする
- [x] アーティストデート軌跡を 〇（実施 / アクセントカラー）・✕（未実施 / グレー）の記号表示に変更する

### 10.5 共通コンポーネント

- [x] 再利用可能なトースト / スナックバーコンポーネントを実装する（`src/components/Toast.tsx`、メッセージ・表示時間をプロパティ化）
- [x] プログラム完遂モーダルコンポーネント（紙吹雪演出付き）を実装する（`src/components/CompletionModal.tsx`）

### 10.6 テスト・検証（手動テスト項目）

- [ ] サインアップ時、利用規約未同意で登録ボタンが非活性、同意で活性になることを確認
- [ ] Day 84 到達ユーザーでログイン/ホーム遷移時に祝福モーダル（紙吹雪）が表示されることを確認
- [ ] ホームの「本日: Day X / 84」と「X/7」表記が経過日数・達成日数と一致することを確認
- [ ] 週移動ボタンの視認性、「現在の週に戻る」ボタンの表示条件（過去/未来週のみ）を確認
- [ ] モーニングページ保存後、`/home` でトースト通知が表示・自動フェードアウトすることを確認
- [ ] 統計画面で日付を単一選択→「詳細」で `/morning-page?date=` へ遷移することを確認
- [ ] 統計のアーティストデート軌跡が 〇/✕ で表示されることを確認
- [ ] 新規アカウント作成 → ログイン → `/home` 遷移の一連を確認
- [ ] ホームの「Day XX / 84」表示が経過日数と一致することを確認
- [ ] dayX（当日 / 過去 / 未来）クリックの挙動を確認（過去は確認ダイアログ経由、未来はロック）
- [ ] `/morning-page?date=...` で対象日のデータが読み込まれることを確認
- [ ] モーニングページ保存後に `/home` へ自動遷移することを確認
- [ ] 保存に成功した日は、ホームの該当 Day ステータスが「完了」に更新されることを確認

---

## 11. 新仕様対応（202606_spec5/direction.md / 画面仕様書 v2.4）

> ベース文書: `docs/202606_spec5/direction.md`、反映先: `docs/screen_spec.md`（v2.4）/ `docs/system_design.md`（v2.3）/ `docs/implementation_plan.md`
> **実装範囲メモ**: spec5 はほぼ**フロントエンド完結**（新規API / スキーマ変更は不要）。「本日」判定はクライアントのシステム日付と各セルの日付（`daily_status[].date` / `daily_activity[].date`）の突合、達成判定は既存 `GET /api/progress` の `morning_pages_this_week` / `artist_date_done` を再利用する。ただし**アーティストデート完了判定を `went_out` のみに変更**したため、バックエンド（`workers/index.js` の `artist_date_done` および統計 `artist_date_weeks`）のロジックのみ調整した（API I/F・スキーマは不変）。
> **演出方針（確定）**: 達成セレブレーションは「控えめ（背景＋豪華アイコン、派手なアニメなし）」かつ「状態ベースでホーム表示のたびに適用」。サマリーカードのみ対象（アクションカードには適用しない）。
> **アーティストデート「完了」判定（確定）**: `went_out`（外出したか）**のみ**で判定する。`excited`（わくわくしたか）は記録するが完了判定には含めない。バックエンドの `GET /api/progress` の `artist_date_done`（`workers/index.js`）および統計の `artist_date_weeks` 集計を `went_out` のみに変更済み。

### 11.1 `/statistics`（統計画面）ヒートマップへの本日表示

- [x] 日次アクティビティヒートマップで本日のセルを判定する（システム日付 × `daily_activity[].date` の突合）
- [x] 本日セルの枠線をアクセントカラー（アンバーの border）で強調する＋「今日」バッジ・凡例を追加
- [x] 本日セルのホバー時ツールチップに「今日」を明記する
- [x] 本日がプログラム期間（84日）外の場合はいずれのセルも強調しないことを担保する（一致セルのみ強調）

### 11.2 `/home`（ホーム画面）今週の実行状況の本日強調

- [x] 「今週の実行状況」のドットグリッドで本日に該当する Day を判定する（システム日付 × `daily_status[].date`）
- [x] 本日の要素に「今日」バッジ＋リング（プライマリカラー）を付与する（達成状態の色表現とは独立して重畳）
- [x] 過去週/未来週の閲覧中は本日強調が表示されないこと（現在週表示時のみ・日付不一致で自然に非表示）

### 11.3 `/home`（ホーム画面）過去週UIの整理

- [x] 過去週表示時の「(閲覧のみ)」テキストを削除する（入力ボタンの非活性＝操作制御は従来通り維持）
- [x] 過去週の「過去の記録を閲覧できます」をクリッカブルなテキストリンク（またはボタン）に変更する
- [x] 当該リンク押下で `/statistics` へ遷移するルーティングを設定する
- [x] リンクと分かるスタイル（下線 / プライマリカラー等）を付与する
- [x] 「モーニングページ」の「未完了」ラベルを削除する（アーティストデートの「未完了」は仕様通り残置）

### 11.4 `/home`（ホーム画面）達成セレブレーション演出

- [x] 発火条件（OR）を判定する: ①モーニングページ今週 7/7（`morning_pages_this_week >= 7`）、②アーティストデート完了（`artist_date_done`）
- [x] 該当条件を満たした項目のカード単位で祝福スタイルを適用する（両方達成時は両カード・サマリーカードのみ対象）
- [x] 該当カードの背景を特別感のある色（薄いゴールド / グラデーション）に変化させる
- [x] チェックマークを豪華なアイコン（👑 モーニングページ / 🎉 アーティストデート）に変更する
- [x] 達成状態が続く限りホーム表示のたびに適用する（瞬間検出は行わない・状態ベース）
- [x] アニメーションは付けず静的表現とする（`prefers-reduced-motion` 配慮は演出が静的なため自動的に満たす）
- [x] 「アーティストデート完了」の判定基準を確定する → **`went_out` のみ**（バックエンド変更済み）

### 11.5 共通要素・デザイントークン

- [x] 本日インジケーター（ホーム週次バッジ＋リング / 統計ヒートマップ枠線＋ツールチップ）を実装する
- [x] 達成セレブレーション表現（サマリーカードの達成状態で背景・アイコンを切り替え）を実装する
- [x] v2.4 デザイントークン（本日セル枠線・本日バッジ・達成カード背景・達成アイコン）を反映する

### 11.6 テスト・検証（手動テスト項目）

- [X] 統計ヒートマップで本日のセルが枠線アクセント＋ツールチップ「今日」で識別できることを確認（記録の有無に関わらず）
- [x] ホーム現在週で本日の要素がバッジ「今日」＋リングで強調されることを確認
- [x] 過去週/未来週の閲覧中は本日強調が出ないことを確認
- [x] 過去週表示で「(閲覧のみ)」が表示されず、入力ボタンの非活性は維持されることを確認
- [x] 過去週の「過去の記録を閲覧できます」リンク押下で `/statistics` へ遷移することを確認
- [x] ホームの「モーニングページ」に「未完了」ラベルが表示されないことを確認
- [x] モーニングページ 7/7 達成時に該当カードが祝福スタイル（薄ゴールド背景＋豪華アイコン）になることを確認
- [x] アーティストデート完了時に該当カードが祝福スタイルになることを確認
- [x] 達成状態でホームを再訪/リロードするたびに祝福スタイルが表示されることを確認
- [x] `prefers-reduced-motion` 設定時に演出が控えめ（静的）になることを確認

---

## 12. リリース準備（本番デプロイ）

> 目的: Cloudflare 本番環境で SPA（フロントエンド）と Workers API を正しく配信し、エンドツーエンドで動作させる。

### 12.1 フロントエンド配信の修正（最優先・既知の不具合）

> **不具合**: 現状 `wrangler deploy` は API Worker（`workers/index.js`）のみをデプロイしており、React SPA（`dist/`）が一切配信されない。そのため本番では `/` が Worker の JSON ヘルスチェックを返し、`/login` 等は JSON 404 となり、ランディングページに遷移できない。

- [x] `wrangler.toml` に Static Assets 設定（`[assets] directory = "./dist"` / `not_found_handling = "single-page-application"` / `binding = "ASSETS"`）を追加する
- [x] Worker の `/` ヘルスチェック（`workers/index.js:15-27`）を別パス（例 `/health`）へ移し、`/` を SPA（index.html）に明け渡す
- [x] API（`/api/*`・`/test-d1`）以外のリクエストは静的アセット（`env.ASSETS`）へフォールバックするようルーティングを整理する
- [ ] `docker compose exec web npm run build` の成果物（`dist/`）を含めてホスト側で `wrangler deploy` し、`/`・`/login`・`/home` 等の各ルートが SPA として表示されることを確認する
- [ ] ブラウザのリロード（直接 URL アクセス）でも各ルートが 200 で表示される（SPA フォールバックが効く）ことを確認する

### 12.2 環境変数・シークレット

- [ ] 本番用 `VITE_API_BASE_URL`（API のオリジン）をビルド時に設定する
- [ ] `SUPABASE_JWT_SECRET` を `wrangler secret put` で本番に登録する
- [ ] `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` など本番値を設定する
- [ ] `CORS_ALLOWED_ORIGIN` を本番ドメインに限定する（`*` のままにしない）
- [ ] `.env` / `.dev.vars` が `.gitignore` 済みでコミットされていないことを確認する

### 12.3 データベース（D1）本番準備

- [ ] 本番 D1 データベースを作成し、`wrangler.toml` の `database_id` を本番用に切り替える（または環境別設定にする）
- [ ] 本番に対しマイグレーション（`npm run d1:migrate:prod`）を適用する
- [ ] テストデータ（`test_data_v2.sql`）が本番に投入されないことを確認する

### 12.4 認証・セキュリティ

- [ ] Workers の JWT 署名検証（`workers/index.js` の TODO: `SUPABASE_JWT_SECRET` による検証）を本番向けに実装する
- [ ] Supabase 側で本番ドメインを Redirect URL / Site URL に登録する
- [ ] Google OIDC ログインの本番クライアント設定を確認する

### 12.5 デプロイ手順・最終確認

- [ ] `npm run build` →（フロント＋Worker 同時デプロイ）の手順を `docs/deployment.md` に整理する
- [ ] 本番 URL でログイン〜ホーム〜モーニングページ〜アーティストデート〜統計の一連の動作を確認する
- [ ] 本番 API のヘルスチェック / D1 接続（`/health`・`/test-d1` 相当）を確認する
- [ ] エラー監視・ログ（`wrangler tail` 等）で本番リクエストが正常に処理されることを確認する